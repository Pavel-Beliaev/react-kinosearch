export type PageDataType = {
    [key: string]: {
        title: string;
        heading: string;
        url: string;
    };
};

export type HeaderProps = {
    pathname: string
}

export type ButtonProps = {
    children: string
}

export type FieldsProps = {
    children: string
}